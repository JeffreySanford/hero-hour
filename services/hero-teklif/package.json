{
  "name": "hero-teklif",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "npm install supertest jest supertest --no-save && npx jest --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2",
    "redis": "^5.0.0"
  }
}
